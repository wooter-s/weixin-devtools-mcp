# 通过命令行回放用例

> **来源**: https://developers.weixin.qq.com/miniprogram/dev/devtools/auto/record_cli.html
> **提取时间**: 2026-02-19
> **文档类型**: 小程序
> **导航路径**: 小程序自动化 /录制回放 /命令行调用 /

---

# 通过命令行回放用例

通过工具录制好的用例手动回放成功后，可以通过命令行调用编写脚本实现自动化。

## 简单回放
```bash
# 打开自动化测试窗口

cli auto-replay --project /Users/username/demo

# 打开自动化测试窗口并回放全部测试用例

cli auto-replay --project /Users/username/demo --replay-all
```
其他工具命令行调用可以参考指引：[命令行调用](./../cli.html)

## 自定义配置

工具版本`1.06.2210092`及以上的版本支持传入配置文件路径`--replay-config-path`修改回放使用的模拟器型号/测试账号等
```bash
cli auto-replay --project /Users/username/demo --replay-config-path /Users/username/your/path/autoreplay.config.json --replay-all
```
请注意：
`replay-config-path`应当是合法json文件的路径，支持一下参数：
```text
export interface IWIReplayConfig {
  simulator: {
    device: string // 目前仅支持工具内置的模拟器
  }
  account: string // 测试账号openid，目前仅支持工具内置的测试账号
}
```
`autoreplay.config.json` 文件参考如下:
```text
{
  "simulator": {
    "device":"iPhone 12/13 Pro Max"
  },
  "account": "o6zAJs7UlU8WQeD35GAJZ9kTeAyc"
}
```

## 代码示例
```bash
# 打开自动化测试窗口

cli auto-replay --project /Users/username/demo

# 打开自动化测试窗口并回放全部测试用例

cli auto-replay --project /Users/username/demo --replay-all
```
```bash
# 打开自动化测试窗口

cli auto-replay --project /Users/username/demo

# 打开自动化测试窗口并回放全部测试用例

cli auto-replay --project /Users/username/demo --replay-all
```
```bash
cli auto-replay --project /Users/username/demo --replay-config-path /Users/username/your/path/autoreplay.config.json --replay-all
```
```bash
cli auto-replay --project /Users/username/demo --replay-config-path /Users/username/your/path/autoreplay.config.json --replay-all
```
```text
export interface IWIReplayConfig {
  simulator: {
    device: string // 目前仅支持工具内置的模拟器
  }
  account: string // 测试账号openid，目前仅支持工具内置的测试账号
}
```
```text
export interface IWIReplayConfig {
  simulator: {
    device: string // 目前仅支持工具内置的模拟器
  }
  account: string // 测试账号openid，目前仅支持工具内置的测试账号
}
```
```text
{
  "simulator": {
    "device":"iPhone 12/13 Pro Max"
  },
  "account": "o6zAJs7UlU8WQeD35GAJZ9kTeAyc"
}
```
```text
{
  "simulator": {
    "device":"iPhone 12/13 Pro Max"
  },
  "account": "o6zAJs7UlU8WQeD35GAJZ9kTeAyc"
}
```
